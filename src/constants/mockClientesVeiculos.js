/**
 * Mock legado mantido exclusivamente para compatibilidade do Portal do Cliente (Story 2.6 / AC8).
 * Todas as operações do ERP foram migradas para clientesRepository e veiculosRepository.
 */
export const MOCK_CLIENTES_VEICULOS = []
