# Story 2.2b: Auditoria — Classificação do Ator e Minimização/Retenção de Dados Pessoais (ADR-008)

**Status:** InReview
**Epic:** Epic 2 (Persistência Real Supabase Postgres & RLS)
**Onda:** A — Fundação (complemento da 2.2)
**Autor:** River (@sm)
**Executor:** @data-engineer
**Quality Gate:** @dev
**Dependências:** Story 2.2 (Done), ADR-008

---

## Story

**Como** proprietário responsável pela conformidade LGPD da oficina,
**Quero** que a trilha de auditoria identifique corretamente quem fez cada alteração (usuário, link público, backend ou acesso direto ao banco) e guarde o mínimo de dados pessoais, por prazo definido e com possibilidade de anonimização,
**Para que** o vetor anônimo (links por token) seja rastreável sem ser confundido com operações da própria oficina, e a trilha deixe de acumular cópias permanentes de CPF, telefone e endereço a cada mudança operacional.

---

## Escopo

**Dentro do escopo (IN):**
- Colunas `db_usuario` e `token_acesso_id` em `public.audit_logs`.
- Nova versão de `public.fn_audit_trigger()`: classificação do ator pela claim `role` e diff de colunas no `UPDATE` (ADR-008 §2, §3.1).
- `public.expurgar_audit_logs()` + agendamento mensal via `pg_cron` (ADR-008 §3.3).
- `public.anonimizar_titular_auditoria()` (ADR-008 §3.4).
- Reclassificação da linha legada de 2026-09-26 (ADR-008 §3.5).

**Fora do escopo (OUT):**
- Uso de `app.token_acesso_id` pelas RPCs públicas (Story 2.17) e chamada da anonimização pelos jobs de retenção de cadastros (Stories 2.20a/2.20c).
- Tela de consulta da trilha para o admin.

---

## Acceptance Criteria

1. `public.audit_logs` ganha as colunas `db_usuario text` e `token_acesso_id text` (ambas `NULL` permitido), via `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`.
2. `public.fn_audit_trigger()` (mantendo `SECURITY DEFINER` e `SET search_path = ''`) classifica o ator pela claim `role` de `request.jwt.claims`, conforme a tabela do ADR-008 §2: `authenticated` → `papel_usuario()` (ou `NULL`) com `usuario_id = auth.uid()`; `anon` → `publico_token`; `service_role` → `service_role`; sem claims → `sistema`. Grava `db_usuario = session_user` e `token_acesso_id = current_setting('app.token_acesso_id', true)` (vazio vira `NULL`).
3. Em `UPDATE`, `valor_anterior` e `valor_novo` contêm apenas `id` e as colunas cujo valor mudou; um `UPDATE` em que só `updated_at` mudou (ou nada mudou) **não** gera linha. `INSERT` e `DELETE` continuam gravando a linha completa.
4. É criada `public.expurgar_audit_logs(p_anos integer DEFAULT 5)`, que exclui linhas com `created_at` anterior ao prazo e retorna a quantidade excluída; executável só pelo dono/`service_role` (sem `EXECUTE` para `anon`/`authenticated`). A extensão `pg_cron` é habilitada e um job mensal chama a função.
5. É criada `public.anonimizar_titular_auditoria(p_tabela text, p_registro_id text)`, executável por `authenticated` mas que só age quando `papel_usuario() = 'admin'` (senão erro `insufficient_privilege`); substitui por `"[anonimizado]"` os valores das colunas de identificação pessoal listadas no ADR-008 §3.4 em `valor_anterior`/`valor_novo` das linhas daquele registro, preserva os demais campos, retorna a quantidade de linhas alteradas e registra uma linha `operacao = 'ANONIMIZACAO'`.
6. A linha legada de 2026-09-26 (`tabela = 'funcionarios'`, `usuario_papel = 'publico_token'`, gravada por SQL direto) é reclassificada para `usuario_papel = 'sistema'` na migration, com comentário explicando o motivo.
7. `anon` e `authenticated` continuam sem `INSERT/UPDATE/DELETE/TRUNCATE` em `audit_logs` (regressão da Story 2.2).
8. Teste SQL transacional (`supabase/tests/2.2b_auditoria_ator_lgpd.test.sql`, com `ROLLBACK`) cobre os quatro atores do AC2, o diff do AC3 (incluindo o caso "só `updated_at`"), o expurgo do AC4 e a anonimização e sua recusa para não admin do AC5; o teste da Story 2.2 continua passando (ajustado apenas onde o AC3 muda o conteúdo esperado do `UPDATE`). Migration aplicada via Supabase CLI e testes executados no banco, com evidência no Dev Agent Record.
9. Nenhum arquivo `.sql` novo excede 400 linhas; `npm run lint` e `npm test` sem novos erros.

---

## Tasks / Subtasks

- [x] Task 1 — Migration `supabase/migrations/{timestamp}_auditoria_ator_lgpd.sql` (AC: 1, 2, 3, 6, 7)
  - [x] `ADD COLUMN IF NOT EXISTS` das duas colunas.
  - [x] `CREATE OR REPLACE FUNCTION public.fn_audit_trigger()` com classificação e diff.
  - [x] Reclassificação da linha legada.
- [x] Task 2 — Retenção (AC: 4)
  - [x] `expurgar_audit_logs()` com `REVOKE` de `anon`/`authenticated`.
  - [x] `CREATE EXTENSION IF NOT EXISTS pg_cron` e `cron.schedule` mensal (idempotente: desagendar pelo nome antes de agendar).
- [x] Task 3 — Anonimização (AC: 5)
  - [x] `anonimizar_titular_auditoria()` com checagem de admin e lista de colunas do ADR-008 §3.4.
- [x] Task 4 — Testes (AC: 8, 9)
  - [x] Teste SQL novo; revisar o teste da 2.2 para o novo formato do `UPDATE`.
  - [x] Rodar no PostgreSQL 17 (PGlite) com testes de mutação e aplicar/verificar no Supabase via CLI.

---

## Dev Notes

- **Fonte das decisões:** `docs/architecture/project-decisions/adr-008-auditoria-ator-e-lgpd.md`. Não reinterpretar a tabela de atores nem a lista de colunas pessoais.
- **Claim `role`:** ler de `NULLIF(current_setting('request.jwt.claims', true), '')::jsonb ->> 'role'`. Sem claims (conexão direta) o `current_setting` retorna `NULL` ou string vazia.
- **Diff no `UPDATE`:** comparar `to_jsonb(OLD)` e `to_jsonb(NEW)` chave a chave (`jsonb_each`). `updated_at` fica fora do diff e não conta como mudança: o `created_at` da própria linha de auditoria já data a alteração.
- **Estado atual:** `fn_audit_trigger()` e as 19 triggers da Story 2.2 estão em produção; basta `CREATE OR REPLACE` da função, sem recriar triggers.
- **`pg_cron` no Supabase:** a extensão fica no schema `extensions`/`cron`; o job roda como `postgres`, dono de `audit_logs`. No harness PGlite o `pg_cron` não existe: o agendamento deve ser condicional (`IF EXISTS (SELECT 1 FROM pg_available_extensions WHERE name = 'pg_cron')`).
- **Stub de testes:** `scripts/sql-test-support/stub_supabase_auth.sql` (Story 2.2).

### Riscos
- O diff muda o formato do `UPDATE` na trilha: qualquer leitura futura da trilha deve tratar `valor_anterior`/`valor_novo` como parciais em `UPDATE`.
- Habilitar `pg_cron` é uma mudança de infraestrutura no projeto de produção (reversível com `DROP EXTENSION`).

---

## Testing

- **SQL:** teste transacional novo + teste da 2.2 ajustado, em PostgreSQL 17 (PGlite) e no Supabase via `supabase db query --linked -f`.
- **Mutação:** remover a classificação `service_role`, gravar a linha inteira no `UPDATE`, permitir anonimização para não admin — cada uma deve fazer o teste falhar.
- **Regressão JS:** `npm test` (sem mudança de código JS esperada).

---

## Estimativa

**Tamanho:** M (Médio)
**Justificativa:** SQL concentrado numa função já existente e duas funções novas, mas com teste cuidadoso de quatro atores, diff e anonimização, e uma extensão nova em produção.

## Definition of Done

- [ ] ACs 1–9 implementados.
- [ ] Migration aplicada no Supabase via CLI, com os testes SQL executados no banco.
- [ ] Change Log atualizado.
- [ ] Story validada pelo `@po` antes de `Ready`.

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-09-26 | 1.0 | Story criada em Draft a partir do ADR-008 (decisões sobre SEC-001, SEC-003 e REQ-001 do gate da Story 2.2). | River (@sm) |
| 2026-09-26 | 1.1 | Validated GO (9/10) — Status: Draft → Ready. Conferido no banco de produção (somente leitura): 1 linha legada em `audit_logs` (`funcionarios`/`UPDATE`/`publico_token`), `pg_cron` 1.6.4 disponível e não instalado, 19 triggers da 2.2 ativas. -1 ponto: a lista de colunas pessoais do ADR-008 §3.4 precisará acompanhar as colunas reais de cada story (risco já registrado no ADR). | Pax (@po) |
| 2026-09-26 | 1.2 | Status: Ready → InProgress — início da implementação. | Dara (@data-engineer) |
| 2026-09-26 | 1.3 | Status: InProgress → InReview — migration `20260926140000` (ator por claim, diff no UPDATE, expurgo 5 anos + `pg_cron`, anonimização, reclassificação legada) verificada em PostgreSQL 17 com 10 mutações e aplicada no Supabase via CLI, com os testes 2.2/2.3/2.2b passando no banco. Emenda no ADR-008 §3.6. | Dara (@data-engineer) |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 5.5 (Dara, @data-engineer) — modo YOLO.

### Debug Log References
- **PostgreSQL 17 (PGlite, scratchpad `run22b.mjs`):** stub + todas as migrations até `20260926130000` + uma linha legada `publico_token` (como em produção) → `20260926140000` aplicada **2×** (idempotência) → testes 2.2, 2.3 e 2.2b **passaram**; linha legada reclassificada para `sistema`.
- **10 testes negativos (mutação), todos detectados** pelo teste 2.2b: sem a migration; `service_role` caindo em `sistema`; `UPDATE` gravando a linha inteira; `updated_at` contando como mudança; anonimização sem checagem de papel; sem anonimizar o nome; sem anonimizar o snapshot da OS; expurgo sem prazo; sem `token_acesso_id`.
- Dois bugs do **próprio teste** encontrados e corrigidos durante a bateria: (1) a busca da linha do `UPDATE` por `valor_novo ? 'coluna'` também casava com a linha do `INSERT` (linha completa) — passou a filtrar `operacao = 'UPDATE'`; (2) o caso "só `updated_at` mudou" não mudava nada de fato (o `BEFORE UPDATE` regrava `now()`, constante na transação) — o teste desliga o trigger de `updated_at` por instantes, dentro da transação desfeita.
- **Supabase "Oficina" via CLI:** `migration list` → só `20260926140000` pendente; `db push --linked` aplicado. `db query --linked -f` dos três testes (2.2, 2.3, 2.2b): exit 0, sem `FALHA`. Estado conferido: `cron.job` `expurgar_audit_logs` com agenda `0 6 1 * *` (dia 1, 06:00 UTC = 03:00 BRT); linha legada = `sistema`; `audit_logs` com 1 linha (sem resíduo dos testes); 0 clientes `t22b*`.
- Regressão JS: sem mudança de código JS.

### Completion Notes List
- **`public.audit_ator()`** centraliza a classificação (claim `role`: `authenticated` → papel + uid; `anon` → `publico_token`; `service_role` → `service_role`; sem claims → `sistema`), usada pela trigger e pela anonimização.
- **`fn_audit_trigger()`** nova versão: diff por `jsonb_each` no `UPDATE` (só colunas alteradas + `id`; `updated_at` fora; sem mudança = sem linha); grava `db_usuario = session_user` e `token_acesso_id` de `app.token_acesso_id`. Triggers da 2.2 não foram recriadas (só `CREATE OR REPLACE` da função).
- **`expurgar_audit_logs(p_anos DEFAULT 5)`**: recusa prazo < 1 (`invalid_parameter_value`), só `service_role`/dono. `pg_cron` habilitado e job mensal agendado de forma idempotente (desagenda pelo nome antes).
- **`anonimizar_titular_auditoria()`** + helper `fn_anonimizar_jsonb(jsonb, pessoa)`. **Desvios do texto do AC5, registrados no ADR-008 §3.6 (emenda):** anonimiza também `nome`/`nome_fantasia`/`razao_social` em tabelas de pessoa e `telefone_secundario`; anonimiza `snapshot_cliente` nas linhas da OS do titular; aceita `service_role`/`sistema` além do admin (jobs de retenção do ADR-007). Autenticado sem papel admin continua recebendo `insufficient_privilege`, como o AC5 exige.
- **Reclassificação legada (AC6):** todas as linhas `publico_token` sem `token_acesso_id` → `sistema` (não havia RPC pública antes). Em produção: 1 linha.
- **Teste da 2.2 ajustado** (AC8): a asserção que lia `updated_at` do log passou a verificar que o `UPDATE` registra o nome alterado e **não** o `updated_at`.

### File List
- **[NEW]** `supabase/migrations/20260926140000_auditoria_ator_lgpd.sql` (297L)
- **[NEW]** `supabase/tests/2.2b_auditoria_ator_lgpd.test.sql` (243L)
- **[MOD]** `supabase/tests/2.2_triggers_auditoria.test.sql` — asserção do AC5 da 2.2 adaptada ao diff
- **[MOD]** `docs/architecture/project-decisions/adr-008-auditoria-ator-e-lgpd.md` — §3.6 emenda de implementação
- **[MOD]** `docs/stories/2.2b.auditoria-ator-e-lgpd.md`, `docs/stories/epic-2-INDEX.md`

---

## QA Results

_A preencher pelo @qa após a implementação._
