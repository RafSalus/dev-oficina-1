# Story 2.4: Correção da Suíte de Testes de Guards de Autenticação e Gate de CI do Epic 2

**Status:** Done
**Epic:** Epic 2 (Persistência Real Supabase Postgres & RLS)
**Onda:** A — Fundação
**Autor:** River (@sm)
**Executor:** @dev
**Quality Gate:** @qa
**Dependências:** Story 2.1

---

## Story

**Como** engenheiro que depende da suíte de testes para confiar em mudanças no fluxo de autenticação e autorização,
**Quero** que `tests/unit/auth-guards.test.js` teste o código de produção real (não uma reimplementação da lógica dentro do próprio teste), e que o pipeline de CI rode também os testes/verificações introduzidas pelo Epic 2,
**Para que** uma regressão real em `ProtectedRoute`/`AdminAuthContext` seja detectada pelos testes, em vez de o teste continuar verde porque valida sua própria cópia da lógica.

---

## Acceptance Criteria

1. `tests/unit/auth-guards.test.js` é reescrito para importar e testar as funções reais de decisão de acesso do código de produção (ex.: a lógica hoje duplicada dentro do próprio teste em `checkHorarioOperacional()` e `evaluateRouteAccess()`, linhas 1-40+) — extraindo essas funções do componente `src/components/auth/ProtectedRoute.jsx` para um módulo puro e testável (ex.: `src/components/auth/routeAccessRules.js`) caso ainda não existam como funções isoladas, e importando-as no teste em vez de redefini-las.
2. Após a extração, `ProtectedRoute.jsx` consome as mesmas funções puras do novo módulo, sem alteração de comportamento observável (mesmos redirecionamentos, mesmo tratamento dos papéis `admin`/`secretaria`/`mecanico`/cliente, mesma checagem de horário `08:00–19:00 America/Sao_Paulo` e mesma checagem de `dev_oficina_cliente_ativo`).
3. Todos os casos de teste hoje presentes em `auth-guards.test.js` (rotas públicas, portal cliente, portal gestão, portal secretaria, portal mecânico, janela de horário) continuam existindo e passando, agora exercitando o código real.
4. `tests/unit/auth-seguranca.test.jsx` (criado na Fase 0 de segurança, commit `b006f0d`) é revisado e, se aplicável, consolidado ou referenciado a partir do arquivo de guards para evitar duplicação de fixtures entre os dois arquivos de teste.
5. O workflow de CI (`.github/workflows/*.yml`, introduzido na Story 1.3) é estendido para rodar `npm test` cobrindo os novos testes do Epic 2 (`supabaseHelpers.test.js` da Story 2.1 em diante) sem necessidade de segredos do Supabase real — todos os testes de repositório usam mocks de `supabase-js`, nunca uma conexão de rede real no CI.
6. `npm run lint` e `npm test` executam localmente em menos de 60 segundos (NFR5), sem novos erros; nenhum arquivo novo/modificado excede 400 linhas.

---

## Escopo

**Dentro do escopo (IN):**
- Extração de lógica pura de `ProtectedRoute.jsx` para um módulo testável.
- Reescrita de `auth-guards.test.js` para exercitar o código real.
- Revisão de sobreposição com `auth-seguranca.test.jsx`.
- Extensão do workflow de CI para cobrir os testes do Epic 2 (mock-only, sem segredos reais).

**Fora do escopo (OUT):**
- Reabrir o escopo de negócio das Stories 1.1/1.9 (guards de rota, MFA).
- Qualquer alteração de comportamento observável do `ProtectedRoute`.
- Testes de integração contra um Supabase real no CI (proibido — apenas mocks).

**Nota de dependência:** esta story é pré-requisito operacional do restante do Epic 2 no sentido de que a suíte de guards passa a validar o código real antes que qualquer story de migração de dados seja mesclada — recomenda-se sequenciá-la logo após a 2.1/2.2/2.3, mas não há dependência técnica de dados entre elas.

---

## Tasks / Subtasks

- [x] Task 1 — Extrair lógica pura de `ProtectedRoute.jsx` (AC: 1, 2)
  - [x] Ler `src/components/auth/ProtectedRoute.jsx` por completo e identificar a lógica de decisão de rota/horário hoje embutida no componente.
  - [x] Criar `src/components/auth/routeAccessRules.js` com as funções puras extraídas (nomes preservando os já usados no teste, se fizer sentido: `checkHorarioOperacional`, `evaluateRouteAccess`, ou equivalentes reais do componente).
  - [x] Atualizar `ProtectedRoute.jsx` para importar e usar essas funções.
- [x] Task 2 — Reescrever o teste (AC: 1, 3)
  - [x] Substituir as definições locais em `auth-guards.test.js` por `import` do novo módulo.
  - [x] Confirmar que todos os casos de teste existentes continuam cobertos.
- [x] Task 3 — Consolidar com `auth-seguranca.test.jsx` (AC: 4)
  - [x] Revisar sobreposição de casos entre os dois arquivos; documentar decisão de manter separados (por responsabilidade: guards de rota vs. autorização por `app_metadata`) ou consolidar.
- [x] Task 4 — CI do Epic 2 (AC: 5)
  - [x] Confirmar que o workflow de CI existente já roda `npm test`; adicionar nota/documentação de que testes de repositório Supabase devem sempre mockar `supabase-js` (nenhuma nova dependência de rede no CI).
- [x] Task 5 — Verificação final (AC: 6)
  - [x] `npm run lint && npm test` e medir tempo de execução.

---

## Dev Notes

### Por que esta story existe (contexto herdado do diagnóstico do @dev)
`tests/unit/auth-guards.test.js` hoje contém, dentro do próprio arquivo de teste, uma reimplementação de `checkHorarioOperacional()` e `evaluateRouteAccess()` (comentário no arquivo: "Testable pure logic for operating hours" / "Testable route permission logic") — ou seja, o teste valida uma cópia da regra, não o `ProtectedRoute.jsx` real. Se alguém introduzir um bug no componente real (ex.: trocar `>= 8` por `> 8`), a suíde continua verde. Isso foi identificado como dívida técnica durante a Fase 0 de segurança e registrado explicitamente como pendência a corrigir no Epic 2.

### Escopo desta story vs. Epic 1
Esta story **não** re-abre o escopo de negócio da Story 1.1 (guards de rota) nem da Story 1.9 (MFA) — ambas já implementadas e com status avançado. O objetivo aqui é puramente de qualidade de teste: extrair lógica pura e testável, sem mudar comportamento.

### Arquivos relevantes
- `src/components/auth/ProtectedRoute.jsx` — componente real, contém hoje a lógica não testada diretamente.
- `src/context/AdminAuthContext.jsx` — contexto de autenticação (fail-closed desde a Fase 0), não deve ser alterado nesta story além do necessário para a extração.
- `tests/unit/auth-guards.test.js` — arquivo a corrigir.
- `tests/unit/auth-seguranca.test.jsx` — arquivo de teste da Fase 0, a revisar por sobreposição.

### Riscos
- Extrair lógica de um componente React para um módulo puro pode expor acoplamento oculto com `useState`/`useEffect` que não é trivial de isolar — se a lógica de decisão depender de estado assíncrono do contexto de autenticação, documentar a fronteira exata entre "função pura testável" (recebe papel, horário, flags como parâmetros) e "efeito colateral do componente" (buscar sessão, redirecionar via `react-router`).
- Não introduzir dependência de rede real (Supabase) em nenhum teste executado pelo CI — todos os testes desta story e das seguintes usam mocks.

---

## Testing

- **Unitário:** `tests/unit/auth-guards.test.js` reescrito, testando o módulo real `routeAccessRules.js`.
- **Regressão:** suíte completa (`npm test`) permanece verde; nenhum caso de teste é removido, apenas redirecionado para o código real.
- **CI:** confirmar execução do workflow completo (`lint`, `test`, `build`) sem segredos do Supabase real.

---

## Estimativa

**Tamanho:** M (Médio)
**Justificativa:** Extração de lógica de um componente React acoplado a `react-router`/contexto exige cuidado para não introduzir regressão sutil, mesmo sem mudar comportamento observável; volume de testes a reescrever é considerável.

## Definition of Done

- [ ] Todos os ACs 1–6 implementados.
- [ ] Todos os casos de teste hoje existentes continuam passando, exercitando o código real.
- [ ] CI roda os testes do Epic 2 sem segredos do Supabase real.
- [ ] `npm run lint` e `npm test` (< 60s) sem novos erros.
- [ ] Change Log atualizado.
- [ ] Story validada pelo `@po` antes de `Ready`.

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-09-24 | 1.0 | Story criada em Draft a partir do diagnóstico de dívida técnica identificado durante a Fase 0 de segurança. | River (@sm) |
| 2026-09-24 | 1.1 | Sem mudança de conteúdo técnico (nenhum ajuste do `@architect` para esta story); adicionadas seções Escopo, Estimativa e Definition of Done; registrada no INDEX como pré-requisito recomendado (não bloqueante) das demais stories do Epic 2. Status permanece Draft. | River (@sm) |
| 2026-09-24 | 1.2 | Story validada pelo `@po` (`*validate-story-draft`). Critérios de aceite verificados, escopo delimitado, pronta para execução pelo `@dev`. Status alterado para Ready. | Pax (@po) |
| 2026-09-26 | 1.3 | Status: Ready → InProgress — início da implementação. | Dex (@dev) |
| 2026-09-26 | 1.4 | Status: InProgress → InReview — regras de rota extraídas para `routeAccessRules.js` e testadas no código real (9/9 mutações detectadas); `supabase-connection.test.js` deixa de exigir credenciais e rede (o CI do `main` estava quebrado sem `.env`); 225/225 testes com e sem Supabase, 12,6s. | Dex (@dev) |
| 2026-09-26 | 1.5 | QA Gate CONCERNS — Status: InReview → Done. Gate em `docs/qa/gates/2.4-correcao-testes-guards-e-ci-epic2.yml`. | Quinn (@qa) |
| 2026-09-26 | 1.5.1 | Correções pós-gate: TEST-001 (testes de renderização do `ProtectedRoute` passam a checar o texto de cada bloqueio, o `signOut` do botão "Sair da Conta", `returnUrl` com query e codificação, `DEV` falso/verdadeiro com `vi.stubEnv`, e a delegação real de `isHorarioOperacionalOficina` com `vi.setSystemTime`) — as 7 mutações que escaparam no gate agora são detectadas, inclusive o fail-open `isDev: true`; REL-001 (CI zera as 6 variáveis lidas por `src/lib/supabase.js`, com e sem prefixo `VITE_`). `auth-guards.test.jsx` com 35 testes; suíte 230/230 com e sem Supabase. | Dex (@dev) |

---

## Dev Agent Record

### Agent Model Used
Claude Opus 5.5 (Dex, @dev) — modo YOLO.

### Debug Log References
- Baseline: `npm test` 29 arquivos / 204 testes (com `.env` local).
- **Descoberta — CI do `main` já quebrado:** com `VITE_SUPABASE_URL=''`/`VITE_SUPABASE_ANON_KEY=''` (condição do CI, que não tem `.env`), 4 testes de `tests/unit/supabase-connection.test.js` falhavam **também no código anterior a esta story** (verificado com `git stash`). O teste exigia as credenciais reais (URL e chave publicável fixadas no código) e chamava o Supabase de verdade via `testarConexaoSupabase()` — viola o AC5.
- **Testes de mutação no código real** (`auth-guards.test.jsx` deve falhar; código restaurado após cada uma) — 9/9 detectadas:
  - `routeAccessRules.js`: `hora >= 8` → `> 8` (o exemplo das Dev Notes); `hora < 19` → `<= 19`; sem bypass do admin; sem trava de dispositivo; `unconfigured` liberado fora de dev; mecânico na Gestão sem redirect.
  - `ProtectedRoute.jsx`: ignora o bloqueio; não consulta o horário; forja cliente ativo.
- Final: `npm test` **29 arquivos / 225 testes** verdes **com e sem** as variáveis do Supabase (CI simulado), `real 0m12.6s` (< 60s, NFR5); `npm run lint` 0 erros em 3,6s (139 warnings pré-existentes); `npx eslint --quiet src/ tests/` 0 erros.

### Completion Notes List
- **AC1/AC2:** criado `src/components/auth/routeAccessRules.js` com `isHorarioOperacional(data)`, `avaliarAcessoRota({...})` e `lerClienteAtivo()`. A decisão devolve `{acao: 'liberar'}`, `{acao: 'redirecionar', destino}` (com `returnUrl` já montado) ou `{acao: 'bloquear', motivo}`. `ProtectedRoute.jsx` (146L → 68L) só coleta contexto (sessão, `localStorage`, `import.meta.env.DEV`) e renderiza a decisão. `AdminAuthContext.isHorarioOperacionalOficina()` passou a delegar para `isHorarioOperacional(new Date())` — uma única regra de horário.
- **Fronteira pura × efeito (Risco das Dev Notes):** puro = decisão a partir de `portal`, `status`, `role`, `returnUrl`, `clienteAtivo`, `isDev`, `isHorario`, `isDispositivo`, `allowedRoles`. Efeito (no componente) = `useAdminAuth()`, `useLocation()`, leitura de `localStorage`, `<Navigate>`/`<BloqueioHorarioView>`. Horário e dispositivo passaram a ser avaliados antes da decisão (antes, só para não-admin); ambos são leituras sem efeito colateral, então o comportamento observável é o mesmo.
- **AC3 — divergência encontrada na cópia antiga:** o caso "mecânico tentando acessar Gestão" esperava `/gestao/acesso-negado`, mas o `ProtectedRoute` real sempre redirecionou mecânico e secretária ao próprio dashboard (`/mecanico/dashboard`, `/secretaria/dashboard`). O teste antigo era verde só porque validava sua própria cópia. O caso foi mantido com a expectativa do comportamento real (sem alterar comportamento). Os outros 14 casos existentes foram mantidos; somados a 15 novos (MFA, `unconfigured` dev/produção, `allowedRoles`, ordem horário→dispositivo, portais cruzados, `lerClienteAtivo`, 6 de renderização do `ProtectedRoute` com as regras reais) = 30 testes.
- **Renomeação:** `auth-guards.test.js` → `auth-guards.test.jsx` (`git mv`), pois os testes de renderização usam JSX e o Vite não transforma JSX em `.js`.
- **AC4:** mantidos separados por responsabilidade — `auth-seguranca.test.jsx` simula o **cliente Supabase** e testa a derivação de sessão/papel/MFA (`app_metadata`, sessão forjada, erro de MFA); `auth-guards.test.jsx` simula o **contexto** e testa as regras de rota. Não há fixtures compartilhadas; referência cruzada no cabeçalho dos dois arquivos.
- **AC5:** o CI já rodava `npm test`. Adicionadas ao passo de testes `VITE_SUPABASE_URL: ''` e `VITE_SUPABASE_ANON_KEY: ''` (nenhum segredo configurado no repositório chega aos testes) e comentário com a regra "mock-only". `supabase-connection.test.js` foi reescrito: recarrega `src/lib/supabase.js` com variáveis simuladas (`vi.stubEnv` + `vi.resetModules`) e `@supabase/supabase-js` mockado; cobre configurado (URL com espaços, chave publishable e anon legada, singleton, healthcheck `conectado`/`erro_auth`/`erro_conexao`) e não configurado (offline, sem criar cliente). Sem credenciais reais nem rede.
- **Observação:** durante a story apareceram alterações que **não são desta story** no working tree (`package.json`, `src/pages/GestaoEntrarPage.jsx`, `src/pages/GestaoAcessoNegadoPage.jsx`, `image.png`); não foram tocadas nem commitadas.

### File List
- **[NEW]** `src/components/auth/routeAccessRules.js` (142L)
- **[MOD]** `src/components/auth/ProtectedRoute.jsx` (146L → 68L)
- **[MOD]** `src/context/AdminAuthContext.jsx` — `isHorarioOperacionalOficina()` delega para `routeAccessRules`
- **[RENAME+MOD]** `tests/unit/auth-guards.test.js` → `tests/unit/auth-guards.test.jsx` (35 testes, código real)
- **[MOD]** `tests/unit/auth-seguranca.test.jsx` — só comentário de referência cruzada
- **[MOD]** `tests/unit/supabase-connection.test.js` — sem credenciais reais nem rede (10 testes)
- **[MOD]** `.github/workflows/ci.yml` — as 6 variáveis do Supabase vazias no passo de testes + nota mock-only
- **[MOD]** `docs/stories/2.4.correcao-testes-guards-e-ci-epic2.md`, `docs/stories/epic-2-INDEX.md`

---

## QA Results

### Review Date: 2026-09-26

### Reviewed By: Quinn (@qa) — revisor independente, commit `6927314`, `git diff main...HEAD`

**Veredito: CONCERNS.** Os ACs 1 a 6 foram atendidos e verificados. Nenhum achado bloqueia. Story movida para Done.

**Evidências reproduzidas pelo QA** (worktrees limpos do commit e de `main`, sem `.env`; as alterações de outra sessão no working tree ficaram fora da revisão)
- `VITE_SUPABASE_URL='' VITE_SUPABASE_ANON_KEY='' VITE_SUPABASE_PUBLISHABLE_KEY='' npx vitest run`: 29 arquivos, 225/225 em 12,3s. O mesmo comando em `main` falha 4 testes de `supabase-connection.test.js` (200/204), o que confirma que o CI de `main` já quebrava sem `.env`.
- Sem rede (`unshare -rn`), com e sem o `.env` real: 225/225. Nenhum teste toca o Supabase.
- `npm run lint`: 0 erros e 129 warnings, a mesma contagem de `main`. Todos os arquivos com até 400 linhas (o maior é `AdminAuthContext.jsx`, com 369).
- Equivalência do `ProtectedRoute` com `main`, ramo a ramo: redirects, `returnUrl`, ordem (loading → cliente → unconfigured+DEV → sessão → MFA → portal → allowedRoles), bypass do admin e horário antes de dispositivo, tudo igual. Antecipar as chamadas de horário e dispositivo não tem efeito observável, porque as duas são leituras sem efeito colateral e com try/catch. A delegação em `AdminAuthContext` não cria ciclo (`routeAccessRules.js` não importa nada).
- AC3: os 15 casos antigos continuam presentes e passando. A nova expectativa "mecânico na Gestão → `/mecanico/dashboard`" está correta frente a `main:src/components/auth/ProtectedRoute.jsx:95`.
- Mutações do QA: 12/12 mortas em `routeAccessRules.js`. No `ProtectedRoute`/`AdminAuthContext`, só 1 de 8 morreu (cliente forjado) e 7 sobreviveram (TEST-001).
- CodeRabbit CLI ausente.

**Achados**
- TEST-001 (medium): a ligação do `ProtectedRoute` com as regras ficou pouco testada. Sobreviveram: a troca dos argumentos de horário e dispositivo, o motivo do bloqueio fixo, o `returnUrl` sem query ou sem encode, `isDev: true` (fail-open em produção, invisível porque DEV=true no Vitest), a ausência de `onLogout` e `isHorarioOperacionalOficina()` retornando `true`.
- REL-001 (low): o CI zera só 2 das 6 variáveis lidas por `src/lib/supabase.js`, e o Vitest expõe `SUPABASE_*` sem prefixo. O comentário "garantem" exagera, mas hoje não há risco real.
- MNT-001 (low): `AdminAuthContext` importa de `components/auth/` (dependência de camada invertida).
- DOC-001 (low): checkboxes da DoD desmarcados.

### Gate Status

Gate: CONCERNS → docs/qa/gates/2.4-correcao-testes-guards-e-ci-epic2.yml
