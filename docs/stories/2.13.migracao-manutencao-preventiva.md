# Story 2.13: Migração de Manutenção Preventiva para Supabase Postgres (Nova Tabela)

**Status:** Draft
**Epic:** Epic 2 (Persistência Real Supabase Postgres & RLS)
**Onda:** D — Pátio
**Autor:** River (@sm)
**Executor:** @dev
**Quality Gate:** @data-engineer
**Dependências:** Story 2.6 (Veículos)

---

## Story

**Como** administrador ou secretária que acompanha a "ficha de saúde" dos veículos da carteira de clientes,
**Quero** que os registros de manutenção preventiva (itens do catálogo preventivo e execuções) sejam persistidos em Supabase Postgres,
**Para que** o cálculo de saúde do veículo (`calcularSaudeVeiculo()`) use dados reais e atualizados, compartilhados entre todos os usuários.

---

## Acceptance Criteria

1. É criada a tabela `public.manutencoes_preventivas` (nova), cobrindo os campos manipulados por `src/constants/mockManutencaoPreventiva.js` para registros de execução (`registrarExecucaoPreventiva()`), com FK para `public.veiculos(id)`.
2. O catálogo de itens preventivos (`ITENS_PREVENTIVOS_CATALOGO`) é avaliado quanto à necessidade de virar tabela própria (`public.itens_preventivos_catalogo`) ou permanecer como constante estática em código — decisão do `@data-engineer`: se o catálogo for editável pelo admin na UI, deve virar tabela; se for fixo e só alterado por deploy de código, permanece constante. Documentar a decisão tomada.
3. As funções puras de cálculo de data (`converterDataParaDate`, `adicionarMesesAData`, `calcularDiasRestantes`) **permanecem** em `mockManutencaoPreventiva.js` (ou são movidas para `src/utils/`, seguindo o padrão de extração de utilitários já aplicado na Story 1.11) — não dependem de acesso a dados e não precisam de mudança de assinatura.
4. É criado `src/repositories/manutencaoPreventivaRepository.js` (novo), expondo assíncronamente: `carregarManutencoesPreventivas()`, `carregarVeiculosAtivosPreventiva()` (delegando para `veiculosRepository`, Story 2.6, em vez de `carregarTodosVeiculosDaFrota()` diretamente), `registrarExecucaoPreventiva(dados)`, `atualizarHodometroVeiculo(placa, novoKm)`.
5. `calcularSaudeVeiculo(veiculo, todasPreventivas)` permanece uma função pura que recebe os dados já carregados como parâmetro — o repositório é responsável por buscar `todasPreventivas` antes de chamar essa função; a lógica de cálculo de saúde em si não é alterada (não inventar nova fórmula).
6. `atualizarHodometroVeiculo()` passa a gravar em `public.veiculos.km_atual` (Story 2.6) em vez de em uma cópia separada do veículo dentro deste módulo — elimina uma fonte potencial de divergência de quilometragem entre este módulo e o cadastro de veículos.
7. RLS de `public.manutencoes_preventivas` usa `public.papel_usuario()`: `admin` acesso total; `secretaria`/`mecanico` `SELECT`/`INSERT` (registrar execução de preventiva é operação de balcão/oficina); `UPDATE`/`DELETE` restritos ao `admin`.
8. Triggers de auditoria e `updated_at` anexadas à nova tabela.
9. Teste automatizado cobre: registro de execução preventiva, cálculo de saúde do veículo com dados mockados, atualização de hodômetro refletindo em `veiculos.km_atual`.
10. Par mobile (`src/pages/dashboard/manutencao-preventiva/mobile/MobileManutencaoPreventivaPage.jsx`) verificado.
11. `npm run lint` e `npm test` sem novos erros; arquivos ≤ 400 linhas.

---

## Tasks / Subtasks

- [ ] Task 1 — Ler `mockManutencaoPreventiva.js` por completo e decidir sobre o catálogo de itens (AC: 1, 2)
- [ ] Task 2 — Criar migration da nova tabela + FKs + triggers (AC: 1, 8)
- [ ] Task 3 — Criar `manutencaoPreventivaRepository.js` (AC: 4, 5, 6)
- [ ] Task 4 — Unificar `km_atual` com `veiculosRepository` (AC: 6)
- [ ] Task 5 — RLS (AC: 7)
- [ ] Task 6 — Testes (AC: 9)
- [ ] Task 7 — Par mobile (AC: 10)
- [ ] Task 8 — Qualidade (AC: 11)

---

## Dev Notes

### Estado atual do código
`src/constants/mockManutencaoPreventiva.js` (324 linhas — brownfield doc registra 698, redução após limpeza de mocks). Depende de `carregarTodosVeiculosDaFrota()` (`src/constants/mockClientesVeiculos.js`) — **esta dependência é resolvida pela Story 2.6**, que cria `veiculosRepository.js`; esta story (2.13) deve consumir esse repositório, não o módulo de constants legado.

### Duplicação de quilometragem — problema real a corrigir
`atualizarHodometroVeiculo(placa, novoKm)` hoje provavelmente atualiza o km em uma estrutura própria deste módulo (a confirmar lendo a função completa, linha 258), separada do `km_atual` gravado em `public.veiculos` (Story 2.6). Esta story deve eliminar essa duplicação, gravando sempre em `veiculos.km_atual` via `veiculosRepository`, para que a Agenda, o cadastro de veículos e a ficha de saúde preventiva nunca divirjam sobre a quilometragem atual de um carro.

### `calcularSaudeVeiculo()` — não inventar nova regra
Esta é a função central do módulo (120-258 do arquivo atual, a maior função do arquivo). Não reimplementar do zero — apenas garantir que ela continue recebendo os mesmos formatos de dado que recebia antes (agora vindos do Postgres em vez do `localStorage`), preservando 100% do resultado observável.

### Arquivos afetados
- **[NEW]** `supabase/migrations/{timestamp}_tabela_manutencoes_preventivas.sql`
- **[NEW]** `src/repositories/manutencaoPreventivaRepository.js`
- **[MODIFY]** `src/constants/mockManutencaoPreventiva.js` (mantém apenas funções puras de data e, possivelmente, `calcularSaudeVeiculo`)
- **[MODIFY]** tela desktop de Manutenção Preventiva (confirmar nome do arquivo ao iniciar)
- **[NEW]** `tests/unit/manutencaoPreventivaRepository.test.js`

---

## Testing

- **Unitário:** registro de execução, cálculo de saúde com fixture de dados conhecida, atualização de hodômetro refletida em `veiculos.km_atual` (mock de `veiculosRepository`).
- **Par mobile:** `MobileManutencaoPreventivaPage.jsx`.

---

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-09-24 | 1.0 | Story criada em Draft. | River (@sm) |

---

## Dev Agent Record

### Agent Model Used
_A preencher._

### Debug Log References
_A preencher._

### Completion Notes List
_A preencher._

### File List
_A preencher._

---

## QA Results

_A preencher pelo @qa após a implementação._
